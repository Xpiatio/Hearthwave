# Keep in step with the builder stage in Dockerfile so dev and the production
# bundle are built by the same Node major.
FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
EXPOSE 5173
