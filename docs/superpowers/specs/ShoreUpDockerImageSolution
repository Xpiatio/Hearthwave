let's clean up the docker compose files.  I need the compose files to be so user friendly a senior citizen could get this up and running.  I need everything to be included and given time to download the correct files.  what can be done to do pre-req installs as part of the docker compose file and have everything packaged up.   I have 2 scenarios I want to take into consideration for docker compose.  A server that houses both front end and backend. I also want a solution that has the frontend and immediate TTS processing backend on one server but off process the secondary TTS processing. The secondary TTS process could be CPU or CUDA.  Ultimately I want it to be starting up the docker image that could trigger the install of prereqs and dependencies if they are not found.   And I want you to remove Claude as a contributor on my Github.
